<article>
  <header class="heading">
    <h1>{{article.title}}</h1>
    <p class="timestamp" title="{{article.published_ts}}">{{shortDate article.published_ts}}</p>
  </header>
  {{{article.content}}}
</article>
